// Desktop Layout Handler - Deprecated
// Logic moved to viewManager.js and playerManager.js
// This file is kept for backwards compatibility but does minimal work

import { utils } from "./global.js";

export function initDesktopLayout() {
  // Desktop layout is now handled by viewManager and playerManager
  // This function is kept as a no-op for backwards compatibility
  console.log('Desktop layout initialization delegated to viewManager and playerManager');
}
